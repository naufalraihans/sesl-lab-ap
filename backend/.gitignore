# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Environment file
.env

# Swagger docs auto-generated (DI-COMMIT karena di-import kode `_ "lab-ap/docs"`;
# kalau diignore, build serverless Vercel gagal resolve package ini)

# Temporary uploads for CSV
/tmp/uploads/
tmp/
