# Go workspace
.env

# Catatan kredensial (DB password, service key, dll) — JANGAN ikut repo
data.txt
*.secret
.idea/
.vscode/
.DS_Store
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# Dokumen internal (PRD, ERD, catatan update, panduan deploy) — tidak ikut push repo
updateAndPRDERD/

# Claude Code: konfigurasi lokal & memory — tidak ikut repo
.claude/
memory/

# Dump log build/check & artefak test — bukan source
frontend/build_out.txt
frontend/check_out.txt
frontend/test-results/
frontend/playwright-report/
